name: cross_local_storage_example
description: Demonstrates how to use the cross_local_storage package.

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.7.0'

dependencies:
  flutter:
    sdk: flutter
  cross_local_storage:
    path: ../

dev_dependencies:
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
